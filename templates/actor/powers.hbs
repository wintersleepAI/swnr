{{! powers Tab }}
<section
  class='tab powers sheet-body {{tab.cssClass}}'
  data-group='primary'
  data-tab='powers'
>
  {{#if system.tweak.showCyberware}}
    {{> "systems/swnr/templates/actor/fragments/cyberware-list.hbs" itemList=cyberware  type='cyberware' name='Cyberware' }}
  {{/if}}

  {{! Resource Pools Display }}
  {{#if hasAnyPools}}
  <div class='resource-pools'>
    <div class="pools-container">
      <div class="pools-header">
        <h4><i class="fas fa-battery-three-quarters"></i> Resource Pools</h4>
        {{#if owner}}
        <button type="button" class="pool-manage-btn" data-action="poolManage" title="Manage Pools">
          <i class="fas fa-cog"></i> Manage
        </button>
        {{/if}}
      </div>
      <div class="pools-display">
        {{#each poolGroups}}
        <div class="pool-group">
          <div class="pool-group-header">
            <span class="resource-name">{{resourceName}}</span>
          </div>
          <div class="pools-list">
            {{#each pools}}
            <div class="pool-badge {{#if isEmpty}}empty{{else if isLow}}low{{else if isFull}}full{{/if}} {{#if hasCommitments}}has-commitments{{/if}}" 
                 title="{{key}}: {{current}} available/{{max}} total{{#if committed}} ({{committed}} committed){{/if}} ({{cadence}})" 
                 data-pool-key="{{key}}">
              <span class="pool-label">{{#if (eq subResource "Default")}}{{../resourceName}}{{else}}{{subResource}}{{/if}}</span>
              <span class="pool-values">{{current}}/{{max}}{{#if committed}}<small class="committed-indicator">-{{committed}}</small>{{/if}}</span>
              <div class="pool-bar">
                <div class="pool-fill" style="width: {{percentage}}%"></div>
              </div>
              {{#if hasCommitments}}
              <div class="commitment-details">
                {{#each commitments}}
                <div class="commitment-item" title="{{powerName}}: {{amount}} effort ({{duration}})">
                  <span class="power-name">{{powerName}}</span>
                  <span class="effort-amount">{{amount}}</span>
                  {{#if (eq duration "commit")}}
                  <button type="button" class="release-commitment" data-action="releaseCommitment" 
                          data-pool-key="{{../key}}" data-power-id="{{powerId}}" 
                          title="Release committed effort">
                    <i class="fas fa-times"></i>
                  </button>
                  {{/if}}
                </div>
                {{/each}}
              </div>
              {{/if}}
            </div>
            {{/each}}
          </div>
        </div>
        {{/each}}
      </div>
    </div>
  </div>
  {{else}}
    <div class="no-pools-message">
      <p><i class="fas fa-info-circle"></i> No resource pools available.</p>
      <p><em>Resource pools are granted by Features, Foci, and Edges. Check the Features tab to configure pool grants.</em></p>
    </div>
  {{/if}}


  {{#if system.tweak.showPowers}}
  {{#each powers as |powersByLevel powerType|}}
    <div class="power-type-section">
      <h3 class="power-type-header">
        {{#if (eq powerType "psychic")}}Psychic Powers{{/if}}
        {{#if (eq powerType "art")}}Arts{{/if}}
        {{#if (eq powerType "adept")}}Adept Abilities{{/if}}
        {{#if (eq powerType "spell")}}Spells{{/if}}
        {{#if (eq powerType "mutation")}}Mutations{{/if}}
        {{#if @root.editable}}
          <a
            class='item-control item-create power-type-create'
            title='{{localize "DOCUMENT.Create" type="Power"}}'
            data-action='createDoc'
            data-document-class='Item'
            data-type='power'
            data-system.sub-type='{{powerType}}'
            {{#if (or (eq powerType "art") (eq powerType "mutation"))}}
              data-system.level='0'
            {{else}}
              data-system.level='1'
            {{/if}}
          >
            <i class='fas fa-plus'></i>
            Add {{#if (eq powerType "psychic")}}Power{{/if}}{{#if (eq powerType "art")}}Art{{/if}}{{#if (eq powerType "adept")}}Ability{{/if}}{{#if (eq powerType "spell")}}Spell{{/if}}{{#if (eq powerType "mutation")}}Mutation{{/if}}
          </a>
        {{/if}}
      </h3>
      
      <ol class='items-list power-type-list'>
        {{#if (or (eq powerType "art") (eq powerType "mutation"))}}
          {{! Flat display for arts and mutations - no level headers }}
          {{#each powersByLevel.flat as |item id|}}
            <li
              class='item flexrow power-item'
              data-item-id='{{item._id}}'
              data-drag='true'
              data-document-class='Item'
            >
              <div class='item-name flexrow'>
                <div class='item-image'>
                  <a class='rollable' data-roll-type='item' data-action='roll'>
                    <img
                      src='{{item.img}}'
                      title='{{item.name}}'
                      width='24'
                      height='24'
                    />
                  </a>
                </div>
                <div>{{item.name}}</div>
              </div>
              <div class='item-controls'>
                <a
                  class='item-control item-edit'
                  title='{{localize "DOCUMENT.Update" type="power"}}'
                  data-action='viewDoc'
                >
                  <i class='fas fa-edit'></i>
                </a>
                {{#if @root.editable}}
                  <a
                    class='item-control item-delete'
                    title='{{localize "DOCUMENT.Delete" type="power"}}'
                    data-action='deleteDoc'
                  >
                    <i class='fas fa-trash'></i>
                  </a>
                {{/if}}
              </div>
            </li>
          {{/each}}
        {{else}}
          {{! Level-based display for other power types }}
          {{#each powersByLevel as |powersAtLevel powerLevel|}}
            <li class='flexrow items-header power-level-header'>
              <div class='item-name'>
                {{#if (eq powerLevel "0")}}
                  Unleveled
                {{else}}
                  Level {{powerLevel}}
                {{/if}}
              </div>
              <div class='item-controls'>
                {{! No add button here - use the main type-level button instead }}
              </div>
            </li>
            
            {{#each powersAtLevel as |item id|}}
              <li
                class='item flexrow power-item'
                data-item-id='{{item._id}}'
                data-drag='true'
                data-document-class='Item'
              >
                <div class='item-name flexrow'>
                  <div class='item-image'>
                    <a class='rollable' data-roll-type='item' data-action='roll'>
                      <img
                        src='{{item.img}}'
                        title='{{item.name}}'
                        width='24'
                        height='24'
                      />
                    </a>
                  </div>
                  <div>{{item.name}}</div>
                </div>
                <div class='item-controls'>
                  <a
                    class='item-control item-edit'
                    title='{{localize "DOCUMENT.Update" type="power"}}'
                    data-action='viewDoc'
                  >
                    <i class='fas fa-edit'></i>
                  </a>
                  {{#if @root.editable}}
                    <a
                      class='item-control item-delete'
                      title='{{localize "DOCUMENT.Delete" type="power"}}'
                      data-action='deleteDoc'
                    >
                      <i class='fas fa-trash'></i>
                    </a>
                  {{/if}}
                </div>
              </li>
            {{/each}}
          {{/each}}
        {{/if}}
      </ol>
    </div>
  {{/each}}
  {{/if}}

</section>