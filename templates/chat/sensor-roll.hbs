<div>
  <h4>Sensor Roll as {{ typeDesc }}</h4>
  <span title="{{ skillRollStr }}">{{{ skillRoll }}}</span>
  {{#if opposedRoll}}
  <span> Other Ship ({{ otherDesc }}) </span>
  {{{opposedRoll}}}
  {{/if}}
</div>
